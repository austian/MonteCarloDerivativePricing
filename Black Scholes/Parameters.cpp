#include "Parameters.h"

Parameters::Parameters(double Expiry, double Rate, double Spot, double Vol, long NumberOfPaths, long NumberOfSteps)	:	
Expiry(Expiry), Rate(Rate), Spot(Spot), Vol(Vol), NumberOfPaths(NumberOfPaths), NumberOfSteps(NumberOfSteps) 	{}